# TrafficVision frontend

The React operations client is documented in the project root `README.md`. Use `npm run dev`, `npm run build`, `npm run lint`, and `npm test` from this directory. API requests are proxied to the local FastAPI service at `127.0.0.1:8000`.

