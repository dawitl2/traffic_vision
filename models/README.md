# Local models

Place explicitly licensed custom `.pt` or `.onnx` files here. Do not commit large weights. Record source, version, checksum, model/data license, and validation scope in `THIRD_PARTY_ASSETS.md`.

